{
  "name": "shifr",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev:web": "pnpm --filter @shifr/web dev",
    "dev:api": "pnpm --filter @shifr/api dev",
    "build": "pnpm --filter @shifr/db push && pnpm --filter @shifr/api build && pnpm --filter @shifr/web build",
    "db:push": "pnpm --filter @shifr/db push",
    "typecheck": "pnpm -r --if-present run typecheck"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
